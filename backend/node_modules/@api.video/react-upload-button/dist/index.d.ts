import * as React from 'react';
import { UploadProgressEvent, VideoUploadResponse } from '@api.video/video-uploader';

interface UploadButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
    children: React.ReactNode;
    uploadToken: string;
    style?: React.CSSProperties;
    onUploadProgress?: (progress: UploadProgressEvent) => void;
    onUploadError?: (errorMessage: string) => void;
    onUploadSuccess?: (video: VideoUploadResponse) => void;
}
declare function UploadButton({ children, uploadToken, style, onUploadError, onUploadProgress, onUploadSuccess, ...props }: UploadButtonProps): JSX.Element;
declare namespace UploadButton {
    var displayName: string;
}

export { UploadButton, UploadButtonProps };
